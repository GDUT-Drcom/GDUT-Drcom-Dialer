<Controls:MetroWindow x:Class="Drcom_Dialer.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:Drcom_Dialer.View"
        mc:Ignorable="d"
                                   
        Title="Title" Height="330" Width="300" ResizeMode="CanMinimize"
        Closing="MetroWindow_Closing" Closed="MetroWindow_Closed" StateChanged="MetroWindow_StateChanged" >

    <Window.Resources>
        <ResourceDictionary>
            <Image x:Key="setting" Source="/Drcom-Dialer;component/Resource/Settings.ico" />
        </ResourceDictionary>
    </Window.Resources>

    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button Content="关于" Click="About_Button_Click"/>
            <Button Click="Setting_Button_Click">
                <StackPanel Orientation="Horizontal">
                    <Rectangle Width="20" Height="20"
                               Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}">
                               <Rectangle.OpacityMask>
                                    <VisualBrush Stretch="Fill" Visual="{StaticResource setting}"/>
                               </Rectangle.OpacityMask>
                    </Rectangle>
                </StackPanel>
            </Button>
        </Controls:WindowCommands>
        
    </Controls:MetroWindow.RightWindowCommands>

    <Grid>
        <StackPanel Margin="10,10,0,0" 
                    Height="249" 
                    Width="272"
                    VerticalAlignment="Top" 
                    HorizontalAlignment="Left"  >
            <Image x:Name="image" Source="/Drcom-Dialer;component/Resource/banner.png"/>
            <StackPanel Margin="10,15,10,5" Height="30" Orientation="Horizontal" >
                <Label x:Name="lbl_username" Content="账户:" VerticalAlignment="Center" Margin="10,0,10,0"/>
                <TextBox x:Name="tb_username" Width="180px" VerticalAlignment="Center" Margin="0,1,0,0" BorderThickness="0,0,0,1" TextChanged="tb_username_TextChanged" Controls:TextBoxHelper.Watermark="311500XXXX" />
            </StackPanel>
            <StackPanel Height="30" Orientation="Horizontal" Margin="10,0,10,5">
                <Label x:Name="lbl_password" Content="密码:" VerticalAlignment="Center"  Margin="10,0,10,0"/>
                <PasswordBox x:Name="pb_password" Width="180px" VerticalAlignment="Center" Margin="0,1,0,0" BorderThickness="0,0,0,1" PasswordChanged="pb_password_PasswordChanged" Controls:TextBoxHelper.Watermark="123456" />
            </StackPanel>
            <StackPanel Height="25" Orientation="Horizontal" Margin="10,0,10,5" HorizontalAlignment="Center">
                <CheckBox x:Name="cb_remember" Content="记住密码" VerticalAlignment="Center" Margin="30,0,30,0" Click="cb_remember_Click"/>
                <CheckBox x:Name="cb_autoLogin" Content="自动登录" VerticalAlignment="Center" Margin="30,0,30,0" Click="cb_autoLogin_Click"/>
            </StackPanel>
            <StackPanel Height="25" Orientation="Horizontal" Margin="10,0,10,5" HorizontalAlignment="Center" Visibility="Hidden">

            </StackPanel>
            <StackPanel Height="36" Orientation="Horizontal" Margin="10,0,10,5" HorizontalAlignment="Center">
                <Button x:Name="btn_dial" Content="拨号" Width="150" Click="btn_dial_Click" Style="{StaticResource AccentedSquareButtonStyle}"/>
            </StackPanel>
        </StackPanel>
        <StatusBar Height="30" VerticalAlignment="Bottom" Margin="0">
            <Label x:Name="lbl_status" Content="这是状态栏" Foreground="White" />
        </StatusBar>

    </Grid>
</Controls:MetroWindow>
